name: Legacy Pages workflow disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "GitHub Pages deploys from .github/workflows/deploy.yml"
